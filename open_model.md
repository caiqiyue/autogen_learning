# 本地开源模型快速入门

## Ollama

### 一、 官方文档与学习资源

- **Ollama 官方网站:** https://ollama.com/ (下载安装包和查找模型都在这里)
- **Ollama 官方 GitHub 仓库 (含详细文档):** https://github.com/ollama/ollama

对于日常使用，你其实不需要啃完所有的文档，掌握下面几个核心命令就完全足够了。

------

### 二、 最基础且必须掌握的核心命令

打开你的 Windows 终端（按 `Win + R`，输入 `cmd` 或 `powershell` 回车），你会经常用到以下这 4 个命令：

1. **运行/下载模型：`ollama run <模型名称>`**
   - **作用：** 这是最灵魂的命令。如果你本地没有这个模型，它会自动帮你下载；如果已经有了，它会直接启动并进入对话界面。
2. **查看已下载模型：`ollama list`** (或者 `ollama ls`)
   - **作用：** 看看你本地已经存了哪些模型，以及它们占用的硬盘大小。
3. **单独下载模型：`ollama pull <模型名称>`**
   - **作用：** 仅仅下载模型，不启动对话。适合在后台挂着下载大文件。
4. **删除模型：`ollama rm <模型名称>`**
   - **作用：** 模型往往很大（几GB到几十GB不等），当你的电脑硬盘告急时，用这个命令清理掉不用的模型。

*(注：在 Windows 上，安装 Ollama 后它的后台服务会自动运行，你通常能在电脑右下角的任务栏看到一只小羊驼的图标。只要它在，这些终端命令就能随时使用。)*

------

### 三、 手把手实战：本地运行 Qwen2.5 (3B)

通义千问（Qwen）是非常优秀的开源模型，其中 **3B**（约 30 亿参数）的版本非常适合普通轻薄本或没有顶级独立显卡的 Windows 电脑运行，既聪明又流畅。

**第一步：安装 Ollama**

1. 去官网 https://ollama.com/ 点击 **"Download"**。
2. 选择 **Windows** 版本下载，得到一个 `.exe` 安装包。
3. 双击安装，一路“Next”直到完成。（安装完成后，右下角会出现 Ollama 的图标）。

**第二步：打开终端并运行下载命令** 按 `Win + R` 键，输入 `cmd`，按下回车键打开命令行窗口。 在黑框框里输入以下命令并回车：

Bash

```
ollama run qwen2.5:3b
```

**第三步：等待下载与自动启动**

- 你会看到屏幕上出现下载进度条。Qwen2.5 的 3B 版本大小通常在 2GB 左右，根据你的网速，可能需要几分钟。
- 下载达到 100% 后，终端会自动出现一个 `>>>` 的提示符。这就说明模型已经成功加载到你电脑的内存/显存里了！

**第四步：开始对话** 现在，你可以在 `>>>` 后面直接输入你想问的问题了，比如： `>>> 你好，请用一句话介绍一下你自己。` *(按回车发送，它就会在你的本地电脑上生成回答)*

如果你想退出当前的对话界面，只需输入 `/bye` 然后回车即可。

![{98C7CB5A-8CD6-4628-A815-2728A696BFC4}](E:\autogen_learning\assests\{98C7CB5A-8CD6-4628-A815-2728A696BFC4}.png)







## LiteLLM

### 一、 官方文档与学习资源

- **LiteLLM 官方文档:** https://docs.litellm.ai/ （非常详尽，支持了哪些模型、怎么配置都在这里）
- **GitHub 仓库:** https://github.com/BerriAI/litellm

和 Ollama 偏向“底层运行”不同，LiteLLM 偏向“上层调用”。它的核心逻辑就是：**把一切模型都伪装成 OpenAI 的 API。**

------

### 二、 最基础且必须掌握的操作概念

在 Windows 的终端里，或者你的 Python 环境中，你主要会用到以下两类操作：

1. **安装 LiteLLM：`pip install litellm`**
   - **作用：** 它是基于 Python 的工具，所以通过 pip 安装。
2. **在代码中直接调用：`litellm.completion()`**
   - **作用：** 替代原生的 `openai.chat.completions.create`。只需改一下 `model` 参数（比如 `model="ollama/qwen2.5:3b"` 或 `model="gpt-4o"`），其他参数完全不用动。
3. **启动本地代理服务 (Proxy Server)：`litellm --model <提供商>/<模型名>`**
   - **作用：** 这是超级实用的进阶玩法。它会在你本地开一个端口（默认 4000），把你指定的模型转化成标准的 OpenAI 接口。很多开源软件只认 OpenAI 的接口，用这个命令就能轻松“骗”过它们，让它们直接用你本地的开源模型。

------

### 三、 手把手实战：用 LiteLLM 代理你本地的 Qwen2.5

接下来，我们把刚才在 Ollama 里下载好的 `qwen2.5:3b`，通过 LiteLLM 包装成一个标准的 API 服务。这样你的 Python 代码就能极其优雅地调用它了。

**第一步：确保 Python 环境并安装 LiteLLM** 打开你的 Windows 终端（`cmd` 或 `powershell`），确保你已经安装了 Python。然后输入：

Bash

```
pip install litellm
```

**第二步：确保 Ollama 在后台运行** 确认你电脑右下角有 Ollama 的小羊驼图标。它默认会在本地 `11434` 端口提供服务，LiteLLM 需要和这个端口通信。

**第三步：一行命令启动 LiteLLM 代理** 在终端中输入以下命令并回车：

Bash

```
litellm --model ollama/qwen2.5:3b
```

*(注意：前缀 `ollama/` 告诉 LiteLLM 去找本地的 Ollama 服务，后面跟着你下载的模型全名)*

如果你看到终端输出类似 `LiteLLM Proxy running on http://0.0.0.0:4000`，恭喜你，代理服务跑起来了！现在，你的电脑在 4000 端口上，伪装成了一个 OpenAI 的服务器。

**第四步：用 Python 代码进行测试（开一个新终端）** 不要关闭刚才那个运行代理的终端。按 `Win + R` 再开一个新的 `cmd` 窗口。你可以写一个非常简单的 Python 脚本（或者在 Python 交互式环境中执行）：

Python

```
import requests

# 就像调用 OpenAI 的接口一样，只是把地址指向了本地的 4000 端口
url = "http://localhost:4000/v1/chat/completions"
headers = {"Content-Type": "application/json"}
data = {
    "model": "ollama/qwen2.5:3b", # 这里填代理配置的模型名
    "messages": [{"role": "user", "content": "作为销售教练，给我一句鼓励的话。"}]
}

response = requests.post(url, headers=headers, json=data)
print(response.json()['choices'][0]['message']['content'])
```

当你运行这段代码时，请求会发给 LiteLLM，LiteLLM 翻译后发给本地的 Ollama，Ollama 里的 Qwen 生成回答，最后原路返回到你的 Python 脚本里。

### 四、特征

打个比方，如果说 **Ollama 是工厂里的“生产机器”**（负责真正干活、消耗电脑算力去生成文本，需要下载和管理沉重的模型文件），那么 **LiteLLM 就是工厂门口的“前台接待”或者“总调度室”**。

LiteLLM 完全不碰真实的物理模型文件（不下载、不加载、不直接占用你的显存），它只负责“接单”、“翻译”和“派单”。

既然它不干核心的“生成”工作，为什么开发者还会这么依赖它呢？因为当你真正在写代码做项目时，它能为你提供极其强大的**实战支持**：

1. **终极**“翻译官” (抹平不同平台的差异)

现在市面上有成百上千种模型和 API（Ollama 的本地接口、百度的文心、阿里的千问 API、Anthropic 的 Claude 等），它们的请求格式千奇百怪。LiteLLM 把所有这些乱七八糟的接口，**全部强制翻译成了统一的 OpenAI 格式**。 你的代码里只需要写一套对接 OpenAI 的逻辑，就能无缝切换全世界的模型。

2. **高可用与兜底保障** (Fallbacks)

比如在开发一个基于知识库的智能对话系统，或者需要模拟真实客户互动的复杂应用时，系统的稳定性至关重要。最怕的就是后端调用的云端大模型突然宕机、网络超时或者遇到接口限流。 使用 LiteLLM，你可以轻松配置一个**“备胎计划（Fallback）”**：

- **首选**：尝试调用线上最聪明的模型（比如 GPT-4o 或线上的 Qwen-Max）。
- **兜底**：如果线上请求失败或超时，LiteLLM 会**自动、无缝**地将这个请求“路由”给你本地用 Ollama 跑着的 `qwen2.5:3b`。 对于前端用户来说，系统永远在线，完全感知不到底层的切换。

3. **统一记录与负载均衡**

如果你的应用有很多人同时使用，LiteLLM 的代理服务可以帮你自动记录每一笔 API 请求消耗了多少 Token，甚至帮你把大量的并发请求均匀地分配到多个不同的模型节点上去，防止某个单一节点被“挤爆”。
